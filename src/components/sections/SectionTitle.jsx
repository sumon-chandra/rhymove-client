const SectionTitle = ({ value }) => {
  return (
    <div className="lg:pb-16 pb-5 lg:text-7xl text-2xl font-black font-K2D text-center">
      <p>{value}</p>
    </div>
  );
};

export default SectionTitle;
