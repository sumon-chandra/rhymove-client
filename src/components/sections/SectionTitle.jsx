const SectionTitle = ({ value }) => {
  return (
    <div
      data-aos="fade-up"
      className="pb-5 text-2xl font-black text-center lg:pb-16 lg:text-7xl font-K2D"
    >
      <p>{value}</p>
    </div>
  );
};

export default SectionTitle;
